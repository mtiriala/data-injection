apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: 'data-alerts'
    type: file
    disableDelete: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/alerting